# cat=basic/enable; type=boolean; label=Don't include JavaScript libraries: This will prevent the extension from including some libraries before the closing body tag. You can use your own inclusion method instead.
dontIncludeJsLibs = 0